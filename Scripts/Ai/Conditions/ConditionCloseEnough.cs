using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ConditionCloseEnough : Condition
{
    //float distance;
    //public ConditionCloseEnough(AIController controller, GameObject target, float distance, bool flipped = false) : base (flipped)
    //{
    //    this.distance = distance;
    //    expressions = new System.Func<bool> [] { () => Vector3.Distance(controller.transform.position, target.transform.position) <= distance};
    //}
    //public ConditionCloseEnough(AIController controller, BlackboardEntry target, float distance, bool flipped = false) : base(flipped)
    //{
    //    this.distance = distance;
    //    expressions = new System.Func<bool>[] { () => target.value != null &&
    //                                        Vector3.Distance(controller.transform.position, (target.value as Character).transform.position) <= distance };
    //}

    //public ConditionCloseEnough(GameObject obj, GameObject target, float distance, bool flipped = false) : base(flipped)
    //{
    //    this.distance = distance;
    //    expressions = new System.Func<bool>[] { () => Vector3.Distance(obj.transform.position,target.transform.position) <= distance};
    //}

    //public override bool Evaluate()
    //{
    //    return base.Evaluate();
    //}

    //public override string ToString()
    //{
    //    return GetType().Name;
    //}
}
